# 🔐 Streamlit Cloud Secrets 配置指南（2025 更新版）

## ⚠️ 重要提示

如果你在 Settings 页面中只看到 "Appearance" 和 "Theme" 设置，说明 Secrets 配置可能在其他位置。

## 🎯 如何找到 Secrets 配置

### 方法 1: 从应用详情页的菜单进入

1. **访问 Streamlit Cloud Dashboard**
   - https://share.streamlit.io/
   - 登录并找到你的应用

2. **点击应用名称**（不是 "Open app" 按钮）
   - 进入应用详情/管理页面

3. **查找以下位置**：
   - **顶部标签页**：可能有 "Settings", "Secrets", "Deployments", "Activity" 等
   - **左侧菜单**：查找 "⚙️ Settings", "Manage", "Configuration"
   - **右上角菜单**：点击 "⋮" (三个点) → 查找 "Settings", "Manage app", "Edit secrets"

### 方法 2: 直接访问 Secrets URL

尝试在浏览器中直接访问（替换 `your-app-name`）：
```
https://share.streamlit.io/app/your-app-name/secrets
```

或：
```
https://share.streamlit.io/app/your-app-name/settings
```

### 方法 3: 查看应用管理页面

1. 在应用详情页，查找 **"Manage app"** 或 **"App settings"** 按钮
2. 进入后查找 **"Secrets"** 或 **"Environment variables"** 部分

## 📋 需要配置的内容

一旦找到 Secrets 编辑器，添加以下内容：

```toml
UF_LITELLM_BASE_URL = "https://api.ai.it.ufl.edu"
UF_LITELLM_API_KEY = "sk-FEhqmwbGafXtX9sv07rZLw"
```

## 🔍 如果仍然找不到

### 选项 1: 使用应用内的检查功能
- 打开你的应用
- 在侧边栏点击 **"🔍 检查 Secrets 配置"** 按钮
- 这会显示当前配置状态

### 选项 2: 查看 Streamlit Cloud 文档
- 访问：https://docs.streamlit.io/streamlit-community-cloud
- 搜索最新的 Secrets 配置方法

### 选项 3: 检查是否有其他配置方式
- 新版本的 Streamlit Cloud 可能使用不同的配置方法
- 可能改名为 "Environment variables" 或其他名称

## 💡 临时解决方案

即使找不到 Secrets 配置，应用仍然可以工作：
- ✅ 使用 fallback 响应生成学生回复
- ✅ 所有功能都可以正常使用
- ⚠️ 只是不使用 AI 生成，而是使用预设模板

## 📞 需要帮助？

如果以上方法都不行：
1. 查看 Streamlit Cloud 的更新日志
2. 联系 Streamlit 支持
3. 查看社区论坛是否有类似问题
