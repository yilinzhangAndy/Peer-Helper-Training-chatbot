# 🔍 如何判断使用的是哪个模型

## 📊 分类逻辑

系统使用**两级分类机制**：

1. **优先使用**: Hugging Face 模型（你的新训练模型）
2. **备用方案**: 关键词分类器（`SimpleIntentClassifier`）

## 🎯 判断方法

### 方法 1: 查看置信度（最简单）

在应用界面查看意图分类的置信度：

- **置信度 > 0.7** → 使用 **Hugging Face 模型** ✅
- **置信度 < 0.6** → 使用 **关键词分类器** ⚠️

### 方法 2: 查看分类准确性

测试一些复杂句子：

**Hugging Face 模型**（新模型）:
- ✅ 能理解复杂语义
- ✅ 能处理同义词和上下文
- ✅ 对复杂句子的分类更准确

**关键词分类器**（备用）:
- ⚠️ 只匹配关键词
- ⚠️ 可能误分类复杂句子
- ⚠️ 对同义词不敏感

### 方法 3: 运行检查脚本

```bash
cd /Users/zhangyilin/Documents/UF/Ph.D/Chatbot/chatbot
export HF_TOKEN="your-token"
python check_model_status.py
```

### 方法 4: 查看应用日志

在 Streamlit Cloud 或本地运行日志中查找：

- **"HF API" 相关错误** → 可能在使用 fallback
- **没有错误** → 可能在使用 Hugging Face 模型

## 🔄 当前状态

根据代码逻辑：

```python
def analyze_intent(text, intent_classifier, role):
    # 1. 先尝试 Hugging Face API
    try:
        hf_result = hf_classify_via_api(text)
        return hf_result  # 使用新模型
    except:
        pass
    
    # 2. Fallback 到关键词分类器
    result = intent_classifier.classify(text)
    return result  # 使用关键词匹配
```

## ✅ 如何确认新模型已部署

### 检查清单：

1. **模型已上传到 Hugging Face** ✅
   - 访问: https://huggingface.co/zylandy/mae-intent-classifier
   - 确认文件都在

2. **配置正确** ✅
   - `HF_MODEL = "zylandy/mae-intent-classifier"`
   - `HF_TOKEN` 已设置

3. **API 可用**
   - 如果返回 503：模型正在加载（等待后重试）
   - 如果返回 200：模型可用 ✅
   - 如果返回 410/404：API 端点问题（代码已处理）

4. **测试分类结果**
   - 发送复杂句子
   - 查看置信度
   - 如果置信度 > 0.7，说明在使用新模型

## 🧪 测试示例

### 测试 1: 简单句子（两种方法都能分类）

```
"I need help with my classes"
```
- 关键词分类器: 置信度 ~0.3-0.5
- Hugging Face 模型: 置信度 ~0.7-0.9

### 测试 2: 复杂句子（只有模型能准确分类）

```
"I've been contemplating my academic trajectory and wondering how to align my research interests with potential career pathways"
```
- 关键词分类器: 可能误分类，置信度低
- Hugging Face 模型: 准确分类，置信度高

## 💡 如果看到分类结果

**如果网站上显示句子有分类**，说明：

1. ✅ **系统正在工作**
2. ⚠️ **可能是关键词分类器**（如果置信度低）
3. ✅ **可能是新模型**（如果置信度高）

**判断方法**：
- 查看置信度数值
- 测试复杂句子
- 运行 `check_model_status.py`

## 🔧 如果新模型未使用

可能的原因：

1. **API 端点问题**
   - 代码已更新支持多个端点
   - 会自动尝试不同的端点

2. **模型正在加载**
   - 首次调用需要 1-5 分钟
   - 等待后会自动使用新模型

3. **配置问题**
   - 检查 `HF_MODEL` 和 `HF_TOKEN` 是否正确

## 📝 总结

- ✅ **Token 和配置**: 无需更改
- ✅ **代码逻辑**: 优先使用 Hugging Face 模型，失败时使用关键词分类器
- ✅ **判断方法**: 查看置信度（>0.7 = 新模型，<0.6 = 关键词）
- ✅ **测试工具**: 使用 `check_model_status.py` 检查

**如果看到分类结果且置信度高，说明新模型已成功部署！** 🎉
