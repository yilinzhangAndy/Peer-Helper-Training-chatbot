# 🔧 Hugging Face 模型 Inference API 设置指南

## 📋 当前状态

根据测试，你的模型已经成功上传到 Hugging Face：
- ✅ **模型仓库**: `zylandy/mae-intent-classifier`
- ✅ **文件完整**: 所有必需文件都在
- ⚠️ **Inference API**: 返回 404，可能需要启用

## 🔍 问题诊断

### 可能的原因：

1. **Inference API 未启用**
   - Hugging Face 需要为模型启用 Inference API
   - 某些模型需要手动启用

2. **API 端点变更**
   - Hugging Face 正在迁移 API 端点
   - 新端点可能还不支持所有模型

3. **模型文件位置**
   - 文件在 `checkpoint-3146/` 子目录
   - 可能需要移动到根目录（但之前也能工作）

## ✅ 解决方案

### 方法 1: 启用 Inference API（推荐）

1. **访问模型页面**
   - https://huggingface.co/zylandy/mae-intent-classifier

2. **查找 "Hosted inference API" 或 "Inference" 设置**
   - 在模型页面右侧或 Settings 中
   - 查找 "Enable Inference API" 或类似选项
   - 启用它

3. **等待几分钟**
   - 启用后，模型需要加载到 Inference API 服务器
   - 通常需要 1-5 分钟

4. **测试**
   - 在模型页面上使用推理小部件测试
   - 或等待应用自动检测

### 方法 2: 检查模型配置

1. **访问模型 Settings**
   - https://huggingface.co/zylandy/mae-intent-classifier/settings

2. **检查以下设置**：
   - **Inference API**: 确保已启用
   - **Model Card**: 确保有 README.md
   - **Files**: 确保所有文件都在

### 方法 3: 移动文件到根目录（如果方法1不行）

如果 Inference API 无法找到子目录中的文件：

1. **使用 Git 或 Web UI 移动文件**
   - 将 `checkpoint-3146/` 下的文件移动到根目录
   - 参考 `MODEL_UPDATE_SUMMARY.md` 中的步骤

2. **或者创建符号链接**（高级）

## 🧪 验证设置

### 在模型页面测试

1. 访问：https://huggingface.co/zylandy/mae-intent-classifier
2. 查找 **"Hosted inference API"** 或推理小部件
3. 输入测试文本，查看是否能分类

### 使用代码测试

```python
import requests

HF_TOKEN = "your-token"
HF_MODEL = "zylandy/mae-intent-classifier"

headers = {"Authorization": f"Bearer {HF_TOKEN}"}
url = f"https://api-inference.huggingface.co/models/{HF_MODEL}"

resp = requests.post(
    url,
    headers=headers,
    json={"inputs": "I want to learn about research"},
    timeout=60
)

if resp.status_code == 200:
    print("✅ 模型可用！")
    print(resp.json())
elif resp.status_code == 503:
    print("⚠️ 模型正在加载，等待后重试")
else:
    print(f"❌ 错误: {resp.status_code}")
    print(resp.text)
```

## 💡 当前状态说明

**如果看到 "🔄 使用关键词分类器作为备用方案"**：

这**不意味着模型部署失败**，而是：

1. ✅ **模型已成功上传**到 Hugging Face
2. ⚠️ **Inference API 可能未启用**或正在加载
3. ✅ **系统正常工作**，使用关键词分类器作为备用
4. ✅ **功能完整**，所有功能都可以使用

## 📝 下一步

1. **检查模型页面**：https://huggingface.co/zylandy/mae-intent-classifier
2. **查找并启用 Inference API**
3. **等待模型加载**（1-5分钟）
4. **刷新应用**，应该会看到 "✅ Hugging Face 意图分类模型已连接"

## 🔄 临时方案

在 Inference API 设置完成之前：
- ✅ 系统会使用关键词分类器
- ✅ 所有功能都可以正常使用
- ✅ 分类仍然有效（只是准确率可能略低）

设置完成后，系统会自动切换到 Hugging Face 模型。
