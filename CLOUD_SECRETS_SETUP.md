# ☁️ Streamlit Cloud Secrets 配置指南

## ⚠️ 重要提示

**本地配置和云端配置是分开的！**

- ✅ **本地**: 使用 `.streamlit/secrets.toml` 文件（不会同步到云端）
- ✅ **云端**: 需要在 Streamlit Cloud Dashboard 中配置

## 🚀 快速配置步骤

### 步骤 1: 登录 Streamlit Cloud
1. 访问 [Streamlit Cloud](https://share.streamlit.io/)
2. 使用 GitHub 账号登录

### 步骤 2: 找到你的应用
1. 在 Dashboard 中找到你的应用
2. 应用名称应该是: `Peer-Helper-Training-chatbot` 或类似名称
3. 应用 URL: `https://peer-apper-training-chatbot-an46q5yl8sqbcyqchwgnin.streamlit.app/`

### 步骤 3: 打开应用设置
1. 点击应用名称进入详情页
2. 点击右上角的 **"⋮"** (三个点) 菜单
3. 选择 **"Settings"** 或 **"⚙️ Settings"**

### 步骤 4: 配置 Secrets
1. 在 Settings 页面，找到 **"Secrets"** 部分
2. 点击 **"Edit secrets"** 或 **"Secrets"** 按钮
3. 在编辑器中添加以下内容：

```toml
UF_LITELLM_BASE_URL = "https://api.ai.it.ufl.edu"
UF_LITELLM_API_KEY = "sk-FEhqmwbGafXtX9sv07rZLw"
```

### 步骤 5: 保存并重新部署
1. 点击 **"Save"** 保存 Secrets
2. Streamlit Cloud 会自动重新部署应用
3. 等待 1-3 分钟完成部署

## 📋 Secrets 格式

在 Streamlit Cloud Secrets 编辑器中，使用 TOML 格式：

```toml
UF_LITELLM_BASE_URL = "https://api.ai.it.ufl.edu"
UF_LITELLM_API_KEY = "sk-FEhqmwbGafXtX9sv07rZLw"
```

**注意**:
- 不要包含 `[secrets]` 或其他节标题
- 直接写键值对即可
- 字符串用双引号括起来

## ✅ 验证配置

部署完成后，访问应用：
- 如果配置成功，警告消息会消失
- 应用会显示: "✅ UF LiteLLM client initialized"
- 可以使用 API 生成学生回复

## 🔍 故障排除

### 问题 1: 仍然显示 "API 未配置"
**解决方案**:
1. 确认 Secrets 已保存
2. 确认应用已重新部署（查看部署日志）
3. 刷新浏览器页面（清除缓存）

### 问题 2: 找不到 Settings 或 Secrets
**解决方案**:
1. 确认你是应用的所有者或有编辑权限
2. 尝试从应用详情页进入 Settings
3. 或者从 Streamlit Cloud Dashboard 主页面进入

### 问题 3: Secrets 格式错误
**解决方案**:
- 确保使用 TOML 格式
- 不要包含额外的括号或节标题
- 检查引号是否正确

## 📝 当前 API 配置信息

根据之前的配置：
- **Base URL**: `https://api.ai.it.ufl.edu`
- **API Key**: `sk-FEhqmwbGafXtX9sv07rZLw`

## 🔐 安全提示

- ✅ Secrets 在 Streamlit Cloud 中是加密存储的
- ✅ 只有应用所有者可以查看和编辑 Secrets
- ✅ 不要在代码中硬编码 API keys
- ✅ 不要将 secrets.toml 提交到 Git（已在 .gitignore 中）

## 📞 需要帮助？

如果遇到问题：
1. 检查 Streamlit Cloud 部署日志
2. 查看应用中的错误消息
3. 参考 Streamlit Cloud 官方文档
